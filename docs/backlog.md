# Backlog

> **Status:** living document - items are explicitly non-canonical until promoted. The other docs in this folder are also DRAFT; this one is the meta-parking-lot.

Parking lot for open questions, deferred features, and post-1.0 ideas. Move items into proper docs when they crystallize into design decisions.

> **Sky Frogs 2 vision material** lives in [`sky_frogs_2_feedback.md`](./sky_frogs_2_feedback.md) - captured player feedback (Eebag, v1.4.1 run) kept as design input for a possible sequel. **Hold, don't build**; not for the current pack, and not to be filed as issues. The wider evidence base is [`sky_frogs_1_retrospective.md`](./sky_frogs_1_retrospective.md), distilled from the whole Discord; same rule applies. Decisions that have actually been made live in [`sky_frogs_2_decisions.md`](./sky_frogs_2_decisions.md), which is a build-against document, not a hold.

## Open design questions

### Productive Frogs feature requests blocking pack work

- [x] **Bog slime spawning on a void island** - RESOLVED. The starter island is forced to `minecraft:swamp` and the pack adds its own parent via a biome modifier. (Note: the parent is `cave_slime`, not bog_slime - see the parent-species item below; Cave is Tier 1, the starter.)
- [x] **Datapack-driven parent species spawn** - RESOLVED. The pack owns spawning: all 6 PF default slime spawns are disabled, and `add_cave_slime_island.json` adds `productivefrogs:cave_slime` to the swamp island. The earlier "Bog parent free-spawns via PF's shipped swamp spawning" premise is wrong - Cave is the starter, and the pack distributes the other parents via quest-reward frogspawn bottles in their tier chapters.
- [x] **Slime Milker automation hooks** - RESOLVED. PF 1.5.3 shipped the Slime Milker (with JEI pages), retiring the PF-V1 load-bearing risk for Tier 1 scale-up.
- [x] **Configurable Froglight data component schema** - RESOLVED. `productivefrogs:slime_variant` is stable and used across all chain scripts (`cave_slime_chain.js`, `geode_slime_chain.js`, `bog_slime_chain.js`) and the froglight-check quests.
- [x] **Froglight smelting recipes** - RESOLVED. The build relies on PF's per-variant smelts; steel is handled in `steel_slime_infusing.js`. No pack-side codegen script needed (`slime_variant_codegen.py` never built).

### Pack-internal decisions

- [x] **Category-typed frog egg as a PF item** - RESOLVED. PF ships placeable category frog eggs that hatch a tadpole with no priming step. NOTE: the old rationale ("Welcome rewards 2x bog_frog_egg") is superseded by the Cave-starter pivot - Welcome now leads into the Cave tier (Tier 1), so the bootstrap parent is cave_slime, not Bog.
- [x] **Swamp-biome island assignment** - BUILT. The SkyblockBuilder starter island is forced to `minecraft:swamp` and the pack's `add_cave_slime_island.json` biome modifier adds cave_slime to it.
- [ ] **Ex Deorum barrel rain-collection mechanic specifics** - confirm the player can place a barrel outside the Skyblock Builder claim and have it accumulate rainwater, and confirm the timing (in-game ticks per fill). Verify during Phase 1 implementation; document in `worldgen.md`.
- [ ] **Slimeball-as-currency creep** - slimeballs are central to multiple loops (frog breeding, slime infusion, vanilla magma cream). Don't make any single loop demand huge quantities or scarcity becomes the bottleneck.
- [~] **Custom Sky Frogs Master Frog trophy item** - MECHANIC BUILT 2026-06-01. Shipped as a KubeJS-defined item `kubejs:master_frog` (`startup_scripts/master_frog_item.js`) crafted in the Extended Crafting Ultimate Table from the Ultimate Singularity + six froglights (`server_scripts/void_recipes.js`); the `master_pond.snbt` capstone hands out the recipe and an epilogue. **Remaining (art only):** the placeholder texture (a PF frog spawn-egg glyph at `kubejs/assets/kubejs/textures/item/master_frog.png`) needs a real 3D model + texture, and an optional Patchouli completion page.
- [ ] **Take Flight's coil quests sit in the wrong order.** The chapter runs Basic Coil -> Off the Ground -> Fuel and Fly -> Advanced Coil, but an **Advanced Coil is a prerequisite** for the Iron Jetpack, not an upgrade after it (the iron cell takes it; see the #233 entry in [`known_issues.md`](./known_issues.md)). The v1.5.2 copy fix patched this in prose ("make one in gold first") and the graph was deliberately left alone, so nothing is blocked. Two loose ends if it is ever revisited: the Advanced Coil task needs a *second* coil crafted, since the first is consumed into the cell, and the chapter still never quests the wood or stone jetpacks the player has to build on the way. Options are re-ordering the chain, adding those two rungs as quests, or shipping `wood.json` with `"disable": true` (which re-maps the ladder so the original one-coil-per-tier framing becomes true). **Q-JETPACK-COIL** will flag whichever claims stop holding.
- [ ] **Take up PF 1.26.0's Slime-in-a-Bucket identity component.** PF #357/#383 made a bucket you scoop off a live slime component-equal to one you craft: **both** of its paths now stamp the flat `productivefrogs:slime_variant`, the same key the Froglight tasks already match on. That fix was filed FOR this pack and names the six gateway quests it unblocks (`road_to_bog` dirt, `road_to_geode` lapis, `road_to_tide` prismarine, `road_to_infernal` netherrack, `road_to_void` ender_pearl, `your_first_iron_ingot` iron), but **neither half of it reaches them yet**, because both halves are pack-side:
  1. **The tasks match the wrong key.** All six declare the NESTED `minecraft:bucket_entity_data={Category,Variant}` under `match_components: "strict"`. Decompiling FTB Quests 2101.1.34 confirms `strict` is `ItemStack.isSameItemSameComponents` - whole-map equality - so a scooped bucket still fails on the live entity state (`Health`, and `NoAI`/`Silent`/`CustomName` when set) that vanilla's `saveDefaultDataToBucketTag` writes and PF deliberately kept. `fuzzy` is the mode that works: it is `allMatch` over the TASK stack's components against the player's, i.e. the task's declared components must be a **subset**, extras ignored. So the six should become `components: { "productivefrogs:slime_variant": "productivefrogs:<v>" }` with `match_components: "fuzzy"` - the shape `take_flight` already uses for `ironjetpacks:jetpack_id`.
  2. **The pack's own buckets would then fail.** Nine server scripts mint slime buckets from a KubeJS item string carrying only `bucket_entity_data` - `froglight_slime_recipes.js` (generated, 103 rows), the four chain scripts, `dissolution_slime_recipes.js`, `iron_slime_bucket.js`, `osmium_slime_bucket.js`, `steel_slime_infusing.js`. None goes through PF's `PFItems.variantSlimeBucket`, so none gets the new component, and under fuzzy-on-`slime_variant` a pack-crafted bucket would stop counting. Both halves have to land together, and `gen_froglight_slime_recipes.py` needs the change at the generator rather than in its output.
  Worth doing as one PR **held for playtest**; note that pre-1.26 buckets already in a world carry no component and will not match (PF says so explicitly), so it wants a changelog line.
- [ ] **Does the Completionist census cover the Rainbow Froglight?** PF 1.26.0's `rainbow` is the first variant primed by a TAG (`c:dyes`) with no `mod_loaded` condition, so it is neither vanilla-by-`primer_item` nor owned by a sister mod, and both census chapters dropped it without a word. It is held out of the singularities by ruling (a dye lane, no smelt result, nothing to compress 1000 of) and is currently held out of the census to match, via `CENSUS_EXCLUDED` in `gen_completionist_chapters.py`. The generator now **aborts** on any future variant that lands in neither census, so this cannot recur silently - but the ruling itself is still open: the census reads as "collect every Froglight", and a player who makes a Rainbow one is not credited for it.
- [ ] **Endgame "creative" tier** - what does the player get after the Master Frog trophy? Sky Bees Reborn has a `creative.snbt` chapter with creative-mode-style infinite resource items. Probably ape the same pattern.
- [ ] **Do we ship Create?** Tracked in [`mod_list.md`](./mod_list.md) - defer to v1.x with a "Create as alternative processing path" chapter rather than a tier.
- [x] **Pink Slime category mapping** - DECIDED AGAINST the Tide proposal. Pink slime is the **Bog capstone**, not a Tide parent: `bog_slime_chain.js` makes plastic -> pink_slime the final step of the Bog organic chain, with IF plastic hard-gated behind the Bog plastic-frog (`if_plastic_gate.js`). Bog won.
- [x] **Obsidian gates the Infernal tier (DECIDED 2026-05-27) - SUPERSEDED 2026-05-31.** The original plan (obsidian via the Cave frog -> build a nether portal -> unlock Infernal) predated PF v1.0.0's species-as-category model. The **built** Infernal tier uses the same Spawnery-primer gate as every other tier: the Tide capstone (nautilus_shell) primes the Spawnery for Infernal Frogspawn - no obsidian portal involved (`road_to_infernal.snbt`). So obsidian is simply **omitted** from the questbook: it's a Cave-category PF variant, but unreachable on skyblock without a diamond pickaxe and not needed for progression. The Cave chain covers iron/copper/gold/coal/redstone (lapis -> Geode); nether materials come from the Infernal frog (netherrack/quartz/glowstone/soul_sand/soul_soil/blaze/netherite_scrap), not from mining obsidian.
- [x] **Chapter order + sugar source (DECIDED 2026-05-27).** IMPLEMENTED. Cave vanilla chain order shipped as iron -> copper -> gold -> coal -> redstone in `cave_slime_chain.js` (prior Slime Milk + 4 stone + 3 sweetslime + a Cave frogspawn bottle per step). Sugar -> sweetslime grant lands in `scaling_the_colony.snbt`. Lapis moved to the Geode tier; obsidian deferred to the Infernal gate; glow_ink_sac omitted. (PF pin history lives in pf_pin_history.md - 1.11.0 as of v0.6.0.) See changelog below.
- [x] **Modded Cave metals -> their tech-mod chapters (DECIDED 2026-05-27).** IMPLEMENTED. The Cave frog produces the modded metals; each gets a one-time bootstrap slime-bucket recipe in the tech-mod chapter that needs it. Osmium's bootstrap (`osmium_slime_bucket.js`) is the opening of the Mekanism chapter, resolving "Cave gating Mekanism." See changelog below.

## Deferred features (post-v0.1)

### Quality-of-life

- **Pack-level difficulty config** - single `config/skyfrogs.json` driving frog spawn rates, milk source counts, sieve drop chances. Useful for "easy/normal/hard" presets and for streamers who want a slower / faster pace.
- **Custom main menu** - branded panorama, custom button colors, version display. Use `packmenu` mod.
- **Custom loading-screen tips** - Tips Mod is shipped; populate with Sky-Frogs-specific tips.
- **Achievement / advancement tree** - vanilla advancements that mirror the questbook. Lower priority since FTB Quests is the canonical progression UI.

### Content

- **Modded Resource Slime variants from Botania, Blood Magic, Create** if we ship those mods later.
- **Custom slime species in our namespace** - `skyfrogs:plasma_slime`, `skyfrogs:rainbow_slime`, etc. for joke variants and challenges.
- **Cross-pack content from Productive Bees** - if we ever decide a hybrid bee+frog pack would be fun, that's a separate pack project (not Sky Frogs).
- **Custom dimension** - "Frog Paradise" - a void dimension reachable post-Tier 6 where frog spawn rates are maximized. Unlock via mass-singularity recipe.

### Infrastructure

- **Server template** - Pterodactyl egg, Docker compose, one-line shell installer.
- **Localization workflow** - Crowdin or Weblate integration for community translations.
- **Anti-cheat hooks** - beyond the fake-player block in `anti.js`, monitor for obvious cheats (e.g., creative-mode flag in survival worlds).

### Marketing / community

- **YouTube playthrough series** - partner with a let's-player for the v1.0 launch.
- **Twitter/Bluesky devlog account** - `@SkyFrogsMC`, post screenshots and changelog excerpts.

## Known risks

- ~~**Productive Frogs V1 might not be feature-complete by Sky Frogs v0.1 target.** Slime Milker is the load-bearing block for the Tier 1 "scale up" beat.~~ RESOLVED: PF 1.5.3 shipped the Slime Milker (with JEI pages), so the scale-up beat is no longer gated.
- **Mod-update churn through NeoForge 1.21.x** could force a re-pin pass on each PF bump. We're now on 1.21.1 - rolled back from 1.21.11 because Ex Deorum and Skyblock Builder have no 1.21.4+ NeoForge builds. Mitigation: hold the pack to whatever MC version the bootstrap-substrate mods are on, not whatever PF is on. PF can be rebuilt to match.
- **CurseForge approval delays** could block v0.1 launch on CF. Since CF is our sole channel, an approval delay = no v0.1 launch. Mitigation: submit the empty CF project for approval as early as possible in the v0.1 cycle so the approval window runs in parallel with content work.
- **License compatibility check** - verify every bundled mod's license allows redistribution in a modpack. Most do (MIT, ARR-with-modpack-permission, GPL, etc.). One or two outliers might require explicit author permission. Audit before v0.1.
- **Productive Frogs is an in-development mod by the pack author**. Risk: pack might inadvertently expose / depend on private APIs that change. Mitigation: pin exact PF version per release; bump deliberately.

## Stretch goals (no commitment)

- **Cross-pack-compatible difficulty modifier** - could the Sky Frogs config layer extend to support modpacks bundling Productive Frogs without the full Sky Frogs treatment? Probably not - this is over-abstraction.
- **Companion website** - `skyfrogs.flatts.example` with progression tree visualization, slime variant browser, mod credits. Cute but low-priority.
- **In-game tutorial NPC** - a villager that walks new players through Tier 0. Hard to make non-annoying; skip.
- **Speedrun category support** - clean kill-the-dragon timing, no-cheats verification. Defer until there's a runner.

## Recently moved out of backlog (changelog)

- **2026-08-16 - SHIPPED: v1.7.0, shaders + a fourteen-mod refresh.** No Productive Frogs bump, so zero roster drift and no generators rerun. **Iris 1.8.14-beta.1** ships client-side (#275, #276), which puts the Shader Packs button in Video Settings; no shader pack is bundled, players bring their own. The pin has to be that beta: Iris mixins straight into Sodium internals and it is the only 1.21.1 build written against Sodium 0.8, while the 1.8.12 stable is built for 0.6.x and the pack cannot go back there (Supplementaries refuses below `0.8.12-beta.1`). **Nothing in either jar's metadata gates that pairing** - 1.8.12 declares an open `sodium [0.6,)` that passes the dependency check and then dies applying mixins, and 1.8.14-beta.1 declares no sodium dependency at all - so the release-checklist dependency sweep cannot clear a future Sodium bump here; only a launch can. Alongside it, `packwiz update --all` moved **14 pins** (FTB Quests, JEI, Supplementaries, Moonlight, Balm, LibX, FancyMenu, FTB XMod Compat, Construction Sticks, SuperMartijn642 Core, Trash Cans, and the Sophisticated set). **Sodium held at 0.8.12** for the second release running, the newer build being a beta and now also coupled to Iris. Apotheosis did not move, so the loader pin stays at **21.1.248**, which is the latest 21.1.x. Trash Cans 1.1.0 is the only player-visible bump: its screen keeps the last six deleted items. **This release got a real launch test**, the gap v1.5.3 shipped with: the dev instance ran on 21.1.248, the world loaded in 68s, `/sf_selftest` passed 45/45, and a shader pack rendered - which is the only way to test the Iris/Sodium pairing, since Fusion and Supplementaries mixin the same Sodium render classes Iris rewires. **Filed during the test: #278** - three Supplementaries cutting-board recipes fail to parse (their results use the pre-1.21 `"item"` shape where 1.21.1 wants `"id"`); one of the three already ships broken in v1.6.2, so the bump widened an existing upstream bug rather than causing it, and the rest of the boot matched the pre-update run class for class with total ERROR lines down 20 to 16. Quest count unchanged at **348**; validator clean; no quest content changed. **Still open:** the post-1.0 items (Master Frog final art, branding gallery, license audit).

- **2026-08-09 - SHIPPED: v1.6.2, both Iron Furnaces Froglight bugs.** Productive Frogs **1.25.2 -> 1.25.4**, two upstream defects of the same class - a component-blind mod treating item identity as item id. **1.25.4 "Second Opinion"**: Iron Furnaces memoised smeltability in a static `Map<Item, Boolean>` living for the whole process, so the first Froglight ever tested answered for every later one; a **variant-less** Froglight (creative tab, or one a Copy-Paste Gadget produced - #249) genuinely has no recipe, cached `false` against the Froglight item, and locked every variant out of every Iron Furnace until the game restarted. That was the pack's most-reported problem: three CurseForge reporters in July and 28 Discord messages from 17 people, with restart-fixes-it as the tell. **1.25.3 "Sorting It Out"**: factory auto-split levelled input slots by item id, averaging counts across slots that each kept their own variant, so 64 of one beside 1 of another came out 32 and 33 - a silent 1:1 converter from the cheapest Froglight to the most expensive, live since v1.4.3 (#225, #220). Iron Furnaces' own bug (upstream Qelifern/IronFurnaces#229, open and unanswered) with no pack-side lever, so PF ships mixins for both, each gated on `ironfurnaces` loading and switchable by config. Plus two Slime Milk placement fixes. Nine other mods to current; Sodium held at 0.8.12. Zero roster drift (singularities 58, froglight recipes 102, census 60/34).

- **2026-08-06 - SHIPPED: v1.6.1, the Skyblock Builder island-creation hotfix.** v1.6.0 had shipped **Skyblock Builder 21.1.32**, whose own regression broke island creation and could drop a player in the wrong dimension - nothing pack-side, and the first thing every new player touches. **21.1.33** fixes both. Six other mods to current (the Sophisticated suite, FTB Library, Supplementaries, Moonlight); loader pin unchanged; Sodium held at 0.8.12. No pack content or quest changes.

- **2026-08-03 - SHIPPED: v1.6.0, a week of Discord reports answered.** **Added:** a quest introducing Productive Frogs' own field guide (#253), which had been craftable and unmentioned, and an optional Bog quest for bees and honey (#255) - honey was reachable the whole time and the silence had convinced players the pack had no bees. **Fixed:** a dedicated server always generates the skyblock now (#251) - the world type lives in `server.properties`, which most hosting panels rewrite from their own defaults, so the pack sets it where the panel cannot overwrite it; two Void quests stopped sending players to prime wild Void Slimes (#252), which the pack's own spawn policy makes non-existent; the Ferricore Ingot quest hands out a Simple Compacting Drawer instead of a three-tier one Ferricore cannot fill (#250); and honeycomb, clay, snowballs, magma cream and prismarine shards now compact in a drawer. The **loader moved to NeoForge 21.1.248** (#266) alongside a 19-mod refresh.

- **2026-07-25 - SHIPPED: v1.5.3, the Ex Deorum grass fix + a full mod-pin refresh.** No Productive Frogs bump, so zero roster drift and no generators rerun. **Ex Deorum 3.11 -> 3.12** is the reason for the release: 3.11 wired all four of its spore items to `CRIMSON_NYLIUM_SPORES_SPREADABLES`, so **Grass Seeds and Mycelium Spores silently did nothing on dirt** - and Grass Seeds are the only grass-block source on a void skyblock. Broken since v1.4.4, across four releases, because nothing in the pack exercised it. Reported on Discord by Sam Gomez; diagnosis in [`known_issues.md`](./known_issues.md). Alongside it, `packwiz update --all` refreshed **34 pins**. **The loader moved: NeoForge 21.1.233 -> 21.1.244**, forced - Apotheosis 8.6.0 and Apothic Attributes 2.10.1 both declare `neoforge [21.1.235,)`, which *refuses to load* rather than warning, so shipping the mod update on the old pin would have bricked the pack. Caught before release by sweeping every jar's declared loader range; verification was static only (loader ranges across all 347 jars, full inter-mod dependency resolution, and a CF export whose manifest declares `neoforge-21.1.244` with all 156 mod refs resolving), because **no in-game launch happened on the new loader** - the dev instance was still on 21.1.230 and `sync_instance.py` syncs jars, not the loader. That gap is the lesson: a NeoForge pin bump needs the instance's modloader changed by hand in the CurseForge app. Also **Sodium 0.8.12-beta.2 -> 0.8.12 stable** (same line; Supplementaries 3.8.4 now hard-declares anything below `0.8.12-beta.1` incompatible, so the 0.8.x rule is enforced by the jar now) and large **Sophisticated Core/Storage/Backpacks** jumps, which touch component-item slot handling - the froglight fragility class from #220/#225, worth watching. Tooling: [`release_checklist.md`](./release_checklist.md) gained a **step 4** with a sweep script so the next `packwiz update` catches a loader-range blocker before a release rather than during one. Quest count unchanged at **346**; validator clean; no quest content changed. **Still open:** the #206 reward tweak (held for playtest); the post-1.0 items (Master Frog final art, branding gallery, license audit).

- **2026-07-25 - SHIPPED: v1.5.2, the Wither Altar unblock + docs housekeeping.** Productive Frogs **1.25.0 -> 1.25.2**. **1.25.2 "Soul Forge"** is the headline and it fixes a live pack bug, not just an upstream one: the **Wither Altar Hatch** and the **Withered Star** each shared a crafting recipe with another item and lost the ambiguity at the table (Hatch vs the End Dragon Altar Hatch, both obsidian + chest; Withered Star vs the Nether Star Catalyst, both obsidian around a Nether Star), so neither was obtainable in survival - and `trophy_pond.snbt` has shipped a Wither Altar quest tasking `productivefrogs:withered_star` since v1.2.0 that **no player could complete**. Both pieces now have distinct recipes. Quest text needed no edit (it says the star is crafted from a Nether Star, still true, and never stated the shape). **1.25.1 "Elbow Room"** adds a Jade readout when a milk source has paused because its slimes are crowded, and counts `maxNearbySlimes` per-resource instead of per-species; the pack pins neither, so both flow through. **Spawn Detective 0.1.0-alpha.2 -> alpha.3**: a green verdict no longer over-promises on a spot the natural spawner rarely reaches (exactly the void-skyblock case), and the mob-cap rows are back on the report screen, Jade, and the command. **Closed #223** - the census generator's lang splice ate 11 hand-authored quest keys on this bump (34 on earlier ones, hand-repaired each time); root cause was its multi-line-array regex branch matching a single-line array's `[` (the form FTB collapses short descriptions to on world save) and running on to the next multi-line `]`, deleting everything between. A census regen is now a true no-op. Also a docs housekeeping pass: em/en dashes swept to ASCII across all 22 affected markdown files, the doc map in `CLAUDE.md` extended to the 7 docs it never listed, a broken relative link fixed, `known_issues.md` restructured (its "Open" section held 13 resolved entries), and the stale status lines in `roadmap.md` / `quest_coverage_audit.md` refreshed. Zero roster drift (singularities 58, froglight recipes 102, census 60/34); quest count unchanged at **346**; no quest content changed. **Still open:** the #206 reward tweak (held for playtest); the post-1.0 items (Master Frog final art, branding gallery, license audit).

- **2026-07-24 - SHIPPED: v1.5.1, the Spawn Detective report-blur hotfix.** A same-day patch on v1.5.0: **Spawn Detective 0.1.0-alpha.1 -> alpha.2**. The Spawn Probe's report screen painted its report and then ran the background pass last, so Minecraft's Menu Background Blur (a full-screen effect, on by default) smeared the report along with the world behind it - the whole thing was only legible with that video setting turned off. The background is now drawn first. Mod-pin only; no pack content, quests, or roster change.

- **2026-07-24 - SHIPPED: v1.5.0, PF 1.25.0 "Second Helpings" + Spawn Detective.** Productive Frogs **1.24.6 -> 1.25.0**, two new automation blocks, both quested. The **Virtual Terrarium** (Processor + glass Display Dome) runs one frog's whole eat-loop hands-off inside two blocks with nothing loose on the ground and no floor space used, tuned by four upgrade slots (Bounty / Appetite / Smelter-or-Melter / RF Overclock); its quest sits in `void_frogs` and is **Void-gated by a KubeJS recipe override** in `void_recipes.js` swapping the stock amethyst blocks for echo shards, which have no source but the Void frog on this skyblock (chosen over a component-froglight ingredient - component-match recipes are unproven here). The **Slime Milk Basin** is a pipeable, leave-alone milk source that waits for a refill when dry instead of draining away; its quest sits in `bog_frogs`, self-gated by its stock packed-mud recipe. The bump folded in **1.24.7 "Closing the Hatch"** (Terrarium Hatch shift-click dupe fix; dispenser-scooped Slime Milk keeps its catalysts and budget). Also added **Spawn Detective** (our own mod, MIT, both-side) with an optional Welcome-chapter quest granting the Spawn Probe, which replays the game's real spawn pipeline to name the rule blocking a mob on a block. Fix: **server packs are now typed as Server Packs on CurseForge** - every release since v0.1.0 attached the server zip, but CurseForge never flagged it as one, which is what hosts read for one-click installs; the pipeline now prints the manual Authors Console step, fails outright if a release ships without a server pack, and `tools/check_server_pack_flag.py` audits published files. 8 new item ids added to `item_ids.txt`; zero roster drift (singularities 58, froglight recipes 102, census 60/34); quest count 343 -> 346. **Dropped by decision (no quest):** Potion of Hopping and Frog Legs/Soup (both push players to kill their own frogs), Tadpole Bucket, Sprinkler redstone switch - see `quest_coverage_audit.md`.

- **2026-07-05 - SHIPPED: v1.4.4, the Productive Frogs 1.24.6 maintenance bump.** Productive Frogs **1.24.5 -> 1.24.6 "Fresh Coat"**, a cosmetic-only texture refresh backported from the 2.x line: sharper Slime Bucket / Tadpole Bucket contents art (each now with a proper bucket base) and upgraded Slime Milk fluid textures, which tint per-variant so every milk benefits. No recipes, data, or saves change - zero roster drift (singularities 58, froglight recipes 102, census 60/34) and no quests touched. Sweep ran clean (synced the dev instance, reran all three generators, validator clean against the 1.24.6 jar). NeoForge pin held at **21.1.233**. Quest count unchanged at 343. **Filed during the sweep: #223** - `gen_completionist_chapters.py`'s lang splice silently drops 34 non-census quest keys on regen (a latent over-matching regex); harmless here because a zero-drift bump means the census is unchanged, so the bad lang change was discarded, but it will bite the next roster-changing bump. **Still open:** the #206 reward tweak (held for playtest); the post-1.0 items (Master Frog final art, branding gallery, license audit).

- **2026-07-04 - SHIPPED: v1.4.3, the editorial rewrite of the rest of the quest book.** Finishes what v1.4.2 started with the Cave tier: every remaining quest description - the Geode, Bog, Tide, Infernal, and Void tiers plus the Terrarium, Trophy Pond, Master Pond, and Completionist chapters - rewritten to the teach/guide voice (shorter and plainer, marketing tone and filler cut, personality moved to titles and subtitles). Several mechanics corrected against the actual mods: Refined Storage cabling is Quartz Enriched Copper (not Iron), the Terrarium's Incubators seed from bottled frog eggs only (not tadpoles), the Flux Dust ritual is built on bedrock, the Dissolution Chamber is the only slime path from the Tide tier onward (old copy said Tier 5), and several recipe and reward names fixed (for example the Simple Compacting Drawer). No pack content, recipes, or progression changed; no quests added or removed - so the **editorial teach/guide pass begun in v1.4.2 now covers every chapter**. (This is the lighter editorial pass, not the deeper spec in `docs/quest_voice_rewrite.md`; whether it settles the "reads as AI" feedback is still an open playtest question.) Quest count unchanged at 343. **Still open:** the #206 reward tweak (held for playtest); the post-1.0 items (Master Frog final art, branding gallery, license audit).

- **2026-07-03 - SHIPPED: v1.4.2, the editorial + maintenance release.** The first three quest chapters (Welcome, Your First Iron Ingot, Scaling the Colony) rewritten to the teach/guide voice, with several mechanics corrected against the actual mods; Froglight names unified to one gold color across every quest; **"Name Your Frogs" now rewards an anvil** instead of name tags (naming is cosmetic, the anvil's ~31-iron cost is steep on a void island, quest stays optional); and a **Spawnery -> Cobblestone quest dependency** added so the frog branch teaches cobble-making before it's needed. Mods: **Productive Frogs 1.24.4 -> 1.24.5 "Deep Breath"** - Resource Frogs and their tadpoles no longer drown underwater (a bug present since the mod's first release); behavior-only, zero roster drift (singularities 58, froglight recipes 102, census 60/34), no quests touched - plus Sophisticated Backpacks + Sophisticated Core bumped to their latest 1.21.1 builds. Quest count unchanged at 343. **Still open:** the #206 reward tweak (held for playtest); the post-1.0 items (Master Frog final art, branding gallery, license audit); the broader quest voice rewrite continues (Cave Frogs onward, `docs/quest_rewrite_candidates.md`).

- **2026-06-30 - SHIPPED: v1.4.1, the Productive Frogs 1.24.4 maintenance bump.** A same-day patch release on top of v1.4.0: Productive Frogs **1.24.3 -> 1.24.4 "Growing Pains"**, a behavior/QoL-only mod update with no new variants, items, or recipes - so zero roster drift (singularities 58, froglight recipes 102, census 60/34) and no quests touched. Fixes upstream: **tadpoles no longer suffocate when they mature in a tight spot** (low ceiling, one-block pool, wedged corner - a frog now appears in the nearest space it fits, or the tadpole waits to grow up; productive-frogs#276), and **tadpoles can be fed Sweetslime to speed growth**, not just slime balls (productive-frogs#277). Sweep ran clean (synced the dev instance, reran all three generators with zero drift, validator clean). NeoForge pin held at **21.1.233** (one patch behind 21.1.234, known-good, a loader bump needs a launch-test). Quest count unchanged at 343. **Still open:** the #206 reward tweak (held for playtest); the post-1.0 items (Master Frog final art, branding gallery, license audit); the broader quest voice rewrite (`docs/quest_rewrite_candidates.md`).

- **2026-06-30 - SHIPPED: v1.4.0, the community-request batch.** Three items from Discord + a maintainer request, all from feedback after v1.3.0. Content: an optional **"Slimes Only"** quest (Cave, next to Players Only) for the Dark Utilities Mob Filter (Slime) - confirmed by decompile that every PF parent/resource slime extends vanilla `Slime` and the filter is an `instanceof Slime` check, so it's a working slime-farm sorter for the frog economy; and **Mini Coal + Mini Charcoal compaction** in Functional Storage drawers (Hullah/Dergib) - Opolis ships these at an 8:1 ratio the drawer's auto-detect skips, so explicit `custom_compacting` tiers register them (coal spans three tiers, charcoal two). Fix: **Better Compatibility Checker** shipped with no `bcc-common.toml`, so its version was a placeholder and the client-vs-server match check always passed green (Hullah, #205) - the pack now ships the config and `release.yml` stamps the version from the git tag into it (and the BBL `/modpack` readout), verified end-to-end on the v1.4.0 export. Quest count 342 -> 343. **Still open:** the #206 reward tweak (a froglight quest reward -> 3-tier compacting drawer, held for playtest); the post-1.0 items (Master Frog final art, branding gallery, license audit); the broader quest voice rewrite (MED/LOW tiers, `docs/quest_rewrite_candidates.md`).

- **2026-06-30 - SHIPPED: v1.3.0, the XP variant + quest-clarity/voice batch.** Productive Frogs **1.24.2 -> 1.24.3 "Muddy Waters"** fixes frog egg-laying on mud / sub-full footing (a footing-aware contact lay; productive-frogs#270/#271), so the pack's own mud-floor breeding guidance is finally correct - no roster drift. Content: the **experience (XP) Void variant** is now craftable + quested (closes the v1.2.0 *Newly deferred* item) - a self-keyed book Dissolution Chamber recipe makes the slime, and an **optional quest forks off the Void chain** (book-infusion / chamber path, smelt to a Bottle o' Enchanting); plus the optional **"Name Your Frogs"** quest (string + paper name tags) from playtest feedback. Polish: a **quest description polish pass** on the 13 wordiest descriptions (trimmed padding, smoothed phrasing, fixed the non-Minecraft "pour" verb, dropped rhetorical-question openers) and **early-game clarity fixes** (the ~24-block spawn distance + the "spawn protection" correction, the mud recipe at point of need, tadpole/breeding quirks, the **Raising the Line** split into action + stats). Quest count 339 -> 342. **Still open (post-1.0):** Master Frog final art, branding gallery, license audit; the broader quest voice rewrite (MED/LOW tiers) tracked in `docs/quest_rewrite_candidates.md`.

- **2026-06-29 - SHIPPED: v1.2.0, the Discord-feedback batch + PF 1.24.2.** Productive Frogs **1.22 -> 1.24.2** (four releases): **1.24.0 "Flip the Switch"** (Terrarium Sprinklers toggle individually with redstone - the upstream answer to standing Discord requests to stage slime production against the mob cap), **1.24.1 "No Crossed Wires"** (the fix the release held for - a Sprinkler signal no longer leaks to its neighbours), **1.24.2 "Name Tags"** (Terrarium "Hatch" -> "Terrarium Hatch", display-only), and **1.23.0 "The Midas Touch"** (opt-in Equivalence/Midas transmutation lane, off by default, + a Wither Altar orientation fix). One new vanilla variant, **experience** (void), tracked in the census; zero other roster drift (singularities 58, froglight recipes 102, census 60/34). New content from the Discord triage: the optional **"Wake the Warden"** quest (Void; the max-Apothic-Enchanting path via the Warden Tendril - closes the long-standing #82 follow-up), the optional **Dragon Altar + Wither Altar** quests (Trophy Pond - the v1.1.0-deferred altar questing, now done), **clay + snow Functional Storage compacting** recipes, and **resource-fit drawer rewards** (froglight quests now reward a drawer matched to each resource's compression depth). Also landed: the **`item_ids.txt` refresh** the v1.1.0 entry deferred (now done via `/sf_dump_items`, 21,460 ids), Terrarium quest-text fixes (placement / height / redstone), and a full **mod-pin refresh** (Sodium on the 0.8.x line, required by Supplementaries). **Newly deferred:** tier-questing the `experience` variant. **Still open (post-1.0):** Master Frog final art, branding gallery, license audit.
- **2026-06-16 - SHIPPED: v1.1.0, a maintenance release.** Productive Frogs **1.21 -> 1.22** ("Apex Predators"): two contained, repeatable boss-farm altars - the **End Dragon Altar** (a hands-off Ender Dragon harvest built on the End exit portal) and the **Wither Altar** (a caged Wither farm in a soul-forged arena) - both harvesting existing boss froglight variants, so zero roster drift (singularities 57, census 34, froglight recipes 101). Fix: the pack's quartz-free storage override had given the **Functional Storage controller and its extension the same recipe** (#186, Discord/tamsen2207), so crafting one could hand back the other and an output-cycling crafting station got stuck; the controllers now use a distinct redstone-block center. **Deferred follow-ups:** questing the two altars (available but unguided), and the `item_ids.txt` refresh for their 10 new item ids (needs an in-game `/sf_dump_items` dump - no quest references them yet, so the validator stays clean).
- **2026-06-14 - SHIPPED: v1.0.0, the 1.0 launch (out of beta).** The pack leaves beta and goes to a full CurseForge release (1.x tags publish as "release", not "beta"), with the client + dedicated-server packs, a full GitHub release, and a Discord announcement. The drop folds in the **quest-text editorial pass** (the whole corpus ground-truthed against the actual mods, issue #169), **Productive Frogs 1.21** ("By the Book"): the in-game **field guide**, the **Lily Pad perch**, and the **breeding rework**; a dedicated **Sky Frogs guide section**, new **Crusher** and **Bio Fuel** Bog quests, and the **Apotheosis Cleaver loot fix**. Post-1.0 SemVer now applies (major = world-breaking, called out loudly; minor = content; patch = fixes). **Waived launch-gate items, now tracked as post-1.0 follow-ups:** the Master Frog final art (still a placeholder texture), the branding gallery (`branding.md`), and the final license audit.
- **2026-06-10 - SHIPPED: v0.12.0, a content-and-polish drop.** **New pack logo/icon** - the frog on a cloud sky with a two-row "SKY FROGS" wordmark in the real Minecraft title style (Minecraft Title Generator, Minecraft Ten + Mangrove, driven via Playwright/Blockbench Web; `pack/icon.png` + `branding/` masters). Productive Frogs **1.17 -> 1.19**: 1.18 "Made to Measure" (pack-author config switches, all kept on), 1.19 "Stone Soup" (Lava Froglights burn as furnace fuel; the Crucible melts cobblestone/stone/gravel/netherrack to lava). New content/QoL: **Spice of Life: Carrot Edition** (food-variety bonus hearts, reward-tuned), **Ender Storage** (color-frequency wireless storage), **craft-any-slime-from-its-Froglight** (a generated shaped recipe per variant - `gen_froglight_slime_recipes.py`; the ingredient form needs an in-world recheck), and a **shapeless 3 gravel -> flint** recipe (#159). Fix: the **fluorite slime recipe** takes the concrete Mekanism gem so JEI shows the calcite -> fluorite -> slime path (#223-adjacent). Also added the `actions/setup-java@v5` CI bump. Open follow-up: confirm the froglight->slime recipes load + strict-match in-world (a 0.12.1 patch if not).
- **2026-06-09 - SHIPPED: v0.11.0, "Frog Legs and Fairy Tales."** Productive Frogs **1.16 -> 1.17**: frog legs/soup (renewable meat, unquested by design - the pack won't push you to kill frogs), the **Frog Net** (early QoL quest in Welcome), the **Froglight Cleaver** (now the Trophy Pond capstone, replacing the old Apex Pond checkmark), and **Princess's Kiss** (Void-tier, dragon -> villager). PF #223 rode along (Crucible plastic/pink_slime melts now yield exactly one bucket), making the **"A Bucket of Latex"** quest (the Fluid Extractor replacement, melt a plastic Froglight in the Crucible) exactly accurate. Added **Apotheosis** (combat/enchanting/loot layer, default config - balance tuning pending playtest) and **Elevators + Time in a Bottle** (Discord QoL). Fixes: **emergency items** now grant an oak sapling (the empty-inventory recovery was wired to nothing), and **mobs no longer spawn with loot-filled backpacks** (a frog-spine bypass; `chance = 0.0`). Open follow-up: bump `actions/setup-java@v4 -> v5` before GitHub's June 16 Node-20 cutover.
- **2026-06-09 - SHIPPED: v0.10.1, a community-feedback polish patch.** The early "Milk It" quest now signposts the Geode Endless Catalyst (then named "Infinite Count"; players were getting stuck on milk depletion, not realizing it becomes infinite - Discord); Terrarium quest clarifications (Sprinkler empty-bucket drain; the 8-frog cap is the incubators' release ceiling, not a cavity wall - lead more in by hand); and the official server (`skyfrogs.benbenlaw.com`) now lists in the multiplayer tab via BBL Core's `config/bbl/core/modpack.toml` (+ `/modpack` and `/discord` commands). Added `docs/release_checklist.md` (per-tag flow + the v1.0 launch gate). Open v1.0 items unchanged (Master Frog art, branding gallery, license audit); launch ops tracked in #141.

- **2026-06-08 - SHIPPED: v0.8.0 -> v0.10.0 (boss farming, JDT, automation, infra).** A fast run of releases on PF 1.13 -> 1.16:
  - **v0.8.0** (PF 1.14): Brewed Froglights (potion auras + Curios slot, quest branch off Blaze), four BOSS resources + the **Trophy Pond** boss chapter (catalyst altars, toxic boss milk as the story beat, More Dragon Eggs to make the egg altar possible), five mob-drop vanilla stragglers, Ultimate Singularity 48 -> 57; **Torchmaster** with a frog-safe spawn-exemption config (#134). Plus the **dedicated-server pack** pipeline (`tools/build_server.py`, boot-verified) shipping to GitHub + CurseForge.
  - **v0.9.x** (PF 1.15): **Just Dire Things** support (4 materials + 3 Crucible fuels, the 7th Sister Ponds column) + the **Slime Churn**; **Polymorphic Refined Storage** (recipe picker in RS grids, #137 - also unblocks the dropped Handcrafted #62); mossy cobblestone re-gated to the Metallurgic Infuser (vanilla/chisel/Ex-Deorum bypasses removed).
  - **v0.10.0** (PF 1.16): **The Terrarium** - a sealed multiblock that automates the frog loop - with its own Infernal chapter (`terrarium.snbt`). Flipped the "hand-operated only" assumption.
  - Docs: added `docs/release_checklist.md` (per-tag + the v1.0 launch gate). Open v1.0 items: Master Frog final art, the branding.md gallery, the final license audit. Open issue: #82 (Warden quests).

- **2026-06-07 - SHIPPED: v0.7.0, the completionist update.** The census chapters land (#121, PR #126): The Whole Pond (50 vanilla froglights drawn as a tree frog) + Sister Ponds (25 modded variants by mod column), generator-built (`tools/gen_completionist_chapters.py`, deterministic ids) in their own **Completionist** chapter group; every modded resource made genuinely craftable (ATO seed chain off osmium, fluorite enriching, orb uraninite - maintainer reversal of the brief only-craftable cut). PF 1.12->1.13: the Froglight Crucible + Casting Mold (the Melting Point chapter, #123), ten new variants, Ultimate Singularity 40 -> 50. The Powered Up chapter shipped (#109, moved to Tide with the chamber); the Mace became obtainable via the chamber-pressed Heavy Core (#127); the FancyMenu title screen landed (#89); flux dust fixed two ways (bedrock island heart + press recipe, #120); Opolis loot boxes fully severed (#115); ghost ATM furnace recipes removed (#116). Validators grew Q-TABLE-CHAIN-MIRROR (#125) + Q-ATO-CHAIN-MIRROR + the generator's loud-failure guards (PR #126 review). Open follow-up: #82 (Warden quests) - the only open issue.
- **2026-06-06 - SHIPPED: v0.6.0, the power-and-polish update.** Powah + Flux Networks land with all 8 PF-1.11 variants live (self-keyed chamber rows - the modded-variant law, PR #106); the blaze resource migrated powder -> rod; two Pillar 1 holes closed (Cobblegen default-deny #90, IF laser drill #107); QoL wave (GraveStone, Inventory Essentials, Cable Facades, Extreme Sound Muffler, Botany Trees + toolless pots); IF Souls returned with a frog-farmable Warden chain; the welcome chapter links the Discord (#111). The three-places audit is now automated in validate_quests.py (#101). Open follow-ups live as GitHub issues per the tracking convention: #82 (Warden quests), #89 (title screen), #108 (ice/snow, blocked on PF#155), #109 (Powah/Flux quest pass).
- **2026-06-01 - BUILT: Tier 6 Void + Extended Crafting (the content campaign's capstone).** Added four chapters (`road_to_void`, `void_frogs`, `the_ultimate_table`, `master_pond`; 28 quests) and the Void mechanical layer. The gate is a one-time **dragon expedition** (no stronghold on a void skyblock, so a 12-frame End portal is crafted from frog-farmed glowstone + soul sand via `void_recipes.js`; enter the End, slay the dragon). Verb = **Extended Crafting** (pinned 7.0.8). The endgame compresses one **Singularity** per vanilla froglight resource (40 in all, generated by `tools/gen_singularities.py` from PF `slime_variant` data; each is 1000 of its smelted resource, since EC's singularity + Quantum Compressor codecs are item/tag-only and cannot match a Froglight variant component) -> the **Ultimate Singularity** (EC's folder-exists guard means exactly these 40 load) -> the **Master Frog** trophy (crafted in a regular table). Dissolution `VOID` row filler = soul soil (Infernal resource; end stone would be circular). Six vanilla Void variants quested; modded Void variants deferred. Closes the "Void verb / filler / primer TBD" open questions. **Merged to `main` 2026-06-02 (PR #47).**
- **2026-05-29 - BUILT: Tier 3 Bog + Industrial Foregoing.** Added the `road_to_bog.snbt` + `bog_frogs.snbt` chapters and `bog_slime_chain.js`. Bog's verb is Industrial Foregoing: plastic is hard-gated behind the Bog plastic-frog (`if_plastic_gate.js`), and pink_slime is the chain capstone (closing the "Pink Slime category mapping" item - Bog won over the Tide proposal). Shipped Industrial Foregoing + IF Souls. Bog chain filler block is mossy cobblestone.
- **2026-05-29 - BUILT: Tier 2 Geode + Mekanism.** Added the `road_to_geode.snbt` + `geode_frogs.snbt` + `mekanism.snbt` chapters and `geode_slime_chain.js` (filler block: gravel). Geode's verb is automation (Mekanism + Generators + Tools). Lapis lands here (moved out of Cave per PF 1.5.1). The Mekanism chapter opens with the osmium slime-bucket bootstrap (`osmium_slime_bucket.js`), so Mekanism hands the player the osmium path itself instead of depending on a missing Cave node. The Mekanism Digital Miner is disabled in `anti.js`. Closes the two DECIDED-2026-05-27 items (chapter order/sugar source + modded-Cave-metals-to-tech-chapters).
- **2026-05-29 - RETROFIT: pack-wide froglight-check principle + per-tier filler blocks.** Per-tier resource quests now detect the variant Froglight (the frog's proof), not the smelted resource (which is gameable). Exceptions: the iron_ingot capstone and the main Mekanism steel quest. Per-tier slime-chain filler blocks settled: Cave = stone, Geode = gravel, Bog = mossy cobblestone.
- **2026-05-28 - BUILT: Tier 1 Cave + Storage and Crafting.** Added `your_first_iron_ingot.snbt`, `scaling_the_colony.snbt`, `cave_frogs.snbt`, `storage_and_crafting.snbt`. Cave is Tier 1 (the starter), with chain order iron -> copper -> gold -> coal -> redstone (`cave_slime_chain.js`, filler block: stone) and the sugar -> sweetslime grant in Scaling the Colony. Cave's verb is crafting upgrades / storage: Sophisticated Storage (bumped) + Functional Storage, with quartz-free networks via `storage_quartz_free.js`. Also shipped cobblegen-galore and All The Ores (ato), and the logs-direct convenience recipes (`log_recipes.js`) + the Squat Grow / FTB Ultimine info quest (both implemented 2026-05-26).
- **2026-05-25 - DONE: Productive Frogs published to CurseForge and added to the pack.** PF shipped a 1.21.1 / NeoForge build as v1.0.1 (project-id 1552728), resolving both the "produce a 1.21.1 PF build artifact" and "publish PF to CurseForge" open items in one move. Added via `packwiz cf add productive-frogs` - pinned at `productivefrogs-1.0.1.jar`, no longer a local-override jar. Clears the v0.1 release gate that blocked shipping. See `docs/{mod_list,roadmap}.md` and `CLAUDE.md` versioning targets.
- **2026-05-23 - DECIDED: Tier 0 bootstrap is the slime farm, not sieving.** Removed sieving from Sky Frogs entirely. Ex Deorum stays in the pack for porcelain bucket + crucibles + barrels (rain-collection barrel is the second-water-source mechanic), but sieves and meshes are KubeJS-disabled in `anti.js`. Rain Shield removed from the pack (conflicted with Ex Deorum barrel rain collection inside the player's claim). First-join grant simplified to saplings + water bucket + lava bucket + food. Tier 0 reshape: cobble gen + second water source + dark-room mob farm + bog-slime farming. The farmed parent is `productivefrogs:bog_slime` (the Bog parent per PF's `ParentSpeciesEntry`); PF v1.0.0 already ships bog_slime spawning for `minecraft:swamp` + `minecraft:mangrove_swamp`, so the pack forces the starter island to `minecraft:swamp` and PF's shipped spawning handles the dark room (no pack-side spawn override). Welcome quest chapter rewards 2× Bog frog egg (breeding pair); cooldown-repeatable emergency replacement quest covers the "frog jumped off the island" case. Closes "Frog source on first launch" and "Bootstrap slimeball source" open questions. See `docs/{worldgen,progression,kubejs_overrides,quest_book}.md`.
- **2026-05-23 - DECIDED: Pack pinned to MC 1.21.1 (rolled back from 1.21.11).** Probed every 1.21.x patch from 1.21.1 through 1.21.11 - Ex Deorum and Skyblock Builder both stuck on the 1.21.1 release with no newer NeoForge builds. Since both are load-bearing (Tier 0 bootstrap + skyblock worldgen respectively), 1.21.1 is the only viable MC version for the pack as designed. Productive Frogs now needs to be rebuilt for 1.21.1 to match (tracked as open item above). Closes the prior "verify Skyblock Builder 1.21.11 release exists before adding" note in mod_list.md - confirmed it doesn't.
- **2026-05-23 - DECIDED: CurseForge-only distribution.** The FTB utility stack (FTB Library / Quests / Teams / Chunks / Ranks / Essentials) is CurseForge-only. `packwiz modrinth export` falls back to inlining the FTB jars as `overrides/mods/*.jar`, which Modrinth's uploader rejects on redistribution policy grounds. Since FTB Quests is the canonical questbook (load-bearing), Modrinth is off the table for v0.1 and the foreseeable future. The same constraint applies to Productive Frogs - it'll publish CF-only too. See `docs/distribution.md`.

When items leave the backlog, log them here briefly so we can see what's been processed.
