# CLAUDE.md

Guidance for Claude Code working in this repo.

## What this is

**Sky Frogs** is a modpack (not a mod) built around [Productive Frogs](../productive-frogs). The pack is **live on CurseForge and content-complete** (**v1.0.0 shipped 2026-06-14, out of beta**; see [`CHANGELOG.md`](./CHANGELOG.md) for the exact release): all six tiers, the Trophy Pond boss campaign, the Terrarium automation chapter, and the Completionist census are built and shipping. The core design is **settled** - frogs are the resource spine, the six-tier order (Cave -> Geode -> Bog -> Tide -> Infernal -> Void) is fixed, the froglight-check law and per-tier verbs are in place. The docs in `docs/` are maintenance reference for that built pack, not exploratory drafts; remaining work is post-1.0 polish: the Master Frog final art (still placeholder), the branding gallery, and the license audit (see [`docs/release_checklist.md`](./docs/release_checklist.md)).

A read-only reference copy of Sky Bees Reborn lives at [`../sky-bees-reborn-reference/extracted/`](../sky-bees-reborn-reference/) as a **study object** - its `kubejs/server_scripts/`, `config/ftbquests/quests/`, and mod selection are useful for seeing how the genre has been solved before. They are **not** templates to mirror. Sky Frogs needs its own answers. When pulling patterns from SBR, attribute the inspiration but justify the choice on Sky-Frogs-specific grounds.

### Sibling repos on this machine

- `../productive-frogs` - the Java mod this pack is built around. File issues there for anything that needs new mod behavior.
- `../spawn-detective` - our own utility mod (CurseForge slug `spawn-detective`, project-id `1621450`, MIT, 1.21.1/NeoForge both-side). A Spawn Probe that replays MC's spawn pipeline to report why a mob won't spawn on a block; bundled in the pack and granted as an optional Welcome-chapter quest reward. File mod-behavior issues there.
- `../sky-bees-reborn-reference/extracted/` - read-only SBR reference. Useful subpaths: `kubejs/server_scripts/anti.js`, `kubejs/server_scripts/productivebees.js`, `config/ftbquests/quests/`, `kubejs/data/productivebees/productivebees/`.

### Doc map (start here)

The docs are living maintenance reference for the built pack. Keep them current when pack content changes (counts, pins, new chapters); log shipped work in [`docs/backlog.md`](./docs/backlog.md) and the [`CHANGELOG.md`](./CHANGELOG.md).

- [`docs/design_overview.md`](./docs/design_overview.md) - concept, core loop, target audience, success criteria
- [`docs/progression.md`](./docs/progression.md) - tier-by-tier player journey (the six PF categories)
- [`docs/mod_list.md`](./docs/mod_list.md) - what ships and why; selection criteria
- [`docs/kubejs_overrides.md`](./docs/kubejs_overrides.md) - the four pillars (anti, parent spawn, slime variants, recipe forcing)
- [`docs/quest_book.md`](./docs/quest_book.md) - FTB Quests chapter outline
- [`docs/patchouli_guide.md`](./docs/patchouli_guide.md) - how the pack extends Productive Frogs' in-game Patchouli guide (`productivefrogs:guide`) with a Sky Frogs category; the resource-pack merge path + the assets-not-junctioned dev caveat
- [`docs/worldgen.md`](./docs/worldgen.md) - void skyblock setup, starter island, parent species distribution
- [`docs/pack_metadata.md`](./docs/pack_metadata.md) - pack identity, versioning policy, asset spec
- [`docs/repo_layout.md`](./docs/repo_layout.md) - packwiz tree, CI shape, helper scripts
- [`docs/distribution.md`](./docs/distribution.md) - CurseForge release workflow
- [`docs/release_checklist.md`](./docs/release_checklist.md) - step-by-step do-list for cutting a release (the PF-bump sweep, version bump, tag, pipeline verify)
- [`docs/curseforge_page.md`](./docs/curseforge_page.md) - public-facing CurseForge listing copy (hook, features, FAQ, mod credits) - edit here first, push to CF
- [`docs/branding.md`](./docs/branding.md) - visual identity spec: category color palette (sourced from PF's `Category.tintArgb()`), asset list, logo/hero composition guidelines
- [`docs/roadmap.md`](./docs/roadmap.md) - phase-by-phase path to the v1.0 launch (shipped 2026-06-14); now tracks post-1.0 work, with explicit user-vs-Claude ownership per deliverable
- [`docs/backlog.md`](./docs/backlog.md) - open questions, deferred features, known risks
- [`docs/pf_pin_history.md`](./docs/pf_pin_history.md) - Productive Frogs bump-by-bump pin history (what each version brought, breaking changes)
- [`docs/known_issues.md`](./docs/known_issues.md) - the bug ledger: symptom, root cause, and the fix, kept after the fix lands so the diagnosis is not re-derived
- [`CHANGELOG.md`](./CHANGELOG.md) - Keep a Changelog format; `[Unreleased]` captures work landed on `main` since last tag

Reference material (consult when working in that area, not part of the pack narrative):

- [`docs/ftbquests_reference.md`](./docs/ftbquests_reference.md) - the FTB Quests SNBT schema: task/reward types, chapter fields, the id rules
- [`docs/quest_testing.md`](./docs/quest_testing.md) - the three validation layers (`validate_quests.py` check ids, CI, the in-game `/sf_selftest`)
- [`docs/voice_and_tone.md`](./docs/voice_and_tone.md) - the pack's writing voice; [`docs/quest_voice_rewrite.md`](./docs/quest_voice_rewrite.md) is the deeper rewrite spec and [`docs/quest_rewrite_candidates.md`](./docs/quest_rewrite_candidates.md) the generated score sheet (`score_quest_voice.py`)
- [`docs/quest_coverage_audit.md`](./docs/quest_coverage_audit.md) - which Productive Frogs content is quested, which is deliberately not, and why
- [`docs/github_issues_best_practices.md`](./docs/github_issues_best_practices.md) - the issue/label convention this repo tracks work with
- [`docs/cf_submission_checklist.md`](./docs/cf_submission_checklist.md) - the one-time CurseForge project submission checklist (distinct from the per-release [`docs/release_checklist.md`](./docs/release_checklist.md))
- [`docs/sky_frogs_2_feedback.md`](./docs/sky_frogs_2_feedback.md) - captured sequel design input; **held, not a build list** (see the backlog)
- [`docs/sky_frogs_1_retrospective.md`](./docs/sky_frogs_1_retrospective.md) - the Sky Frogs 1 lessons-learned pass, distilled from the full community Discord export (3,216 human messages, 430 participants, 2026-06-06 to 2026-08-07); **input for the MC 26.x sequel**, not a change list for this pack

## Versioning targets

- Minecraft: **1.21.1** (rolled back from 1.21.11 on 2026-05-23 - Ex Deorum and Skyblock Builder have no 1.21.4+ NeoForge builds, and they're load-bearing for Tier 0 bootstrap and skyblock worldgen respectively. Productive Frogs needs to be rebuilt for 1.21.1 to match.)
- Loader: **NeoForge 21.1.248** (bumped from 21.1.244 on 2026-08-03 to stay current, not forced: the pin was four patches behind and every one of the 156 pinned jars already resolved against 244, with no mod declaring an upper bound. Earlier, 21.1.244 replaced 21.1.233 on 2026-07-25 because Apotheosis 8.6.0 and Apothic Attributes 2.10.1 both declare `neoforge [21.1.235,)` - the pack would not have loaded. Earlier, 21.1.233 replaced 21.1.230 on 2026-06-10; 230 applied the `GuiGraphics` tooltip patch unreliably on some fresh installs, crashing Apotheosis's required accessor at load. Both bumps were forced by Apotheosis: **check its `neoforge.mods.toml` range whenever it updates** - keep the pin current and off known-bad builds)
- Java: **21**
- Distribution: **CurseForge only.** Modrinth is off the table because the FTB utility stack (FTB Library / Quests / Teams / Chunks / Ranks / Essentials) is CF-only and Modrinth forbids inlining CF jars as overrides. This same constraint applies to Productive Frogs - PF is CF-only too.
- Productive Frogs: **published on CurseForge** (slug `productive-frogs`, project-id `1552728`) as of 2026-05-25. Added via `packwiz cf add productive-frogs`; currently pinned at **`productivefrogs-1.25.2.jar`** (1.21.1 / NeoForge; 2026-07-25 - **Soul Forge** [1.25.2]: **the Wither Altar is craftable again** - the Wither Altar Hatch and the Withered Star each shared a crafting recipe with another item and lost the ambiguity, so both were unbuildable in survival [Hatch lost to the End Dragon Altar Hatch, both obsidian+chest; Withered Star lost to the Nether Star Catalyst, both obsidian around a Nether Star]. The Hatch is now obsidian + soul sand around a chest and the Withered Star is a Nether Star wrapped in soul sand; the two it collided with are unchanged. **This unblocks the pack's already-shipped Wither Altar quest** [`trophy_pond.snbt`, task `productivefrogs:withered_star`], which no player could complete. Quest text still reads true - it says only that the Withered Star is crafted from a Nether Star, not the shape. Earlier, 2026-07-25 - **Elbow Room** [1.25.1]: a Slime Milk source now says on Jade when it has paused because its slimes are crowded ["Paused: 30 / 30 Diamond slimes nearby"], and the `maxNearbySlimes` limit [default 30] now counts only that source's OWN resource instead of its whole species, so packing different resources of one species side by side no longer makes them fight over one shared limit. The pack pins no `maxNearbySlimes`, so both flow through from the jar. Earlier, 2026-07-24 - **Second Helpings** [1.25.0]: two new automation blocks, no new slime variants - the **Virtual Terrarium** [a two-block hidden machine running one frog's whole eat-loop hands-off: Processor + glass Display Dome, netted frog in, its milk in, output out the bottom; four upgrade slots - Bounty/Appetite/Smelter-or-Melter/RF Overclock] and the **Slime Milk Basin** [a pipeable, leave-alone milk source: pour any variant's milk in, it spawns that variant's slimes on the same budget as a placed pool, and waits for a refill when dry instead of draining away]. The 1.24.6 -> 1.25.0 jump also folds in **1.24.7 (Closing the Hatch)**: the Terrarium Hatch shift-click dupe fix and dispenser-scooped Slime Milk keeping its catalysts/budget. Zero roster drift [singularities 58, froglight recipes 102, census 60/34], validator clean, dev instance re-synced to the 1.25.0 jar. **Pack-side: both new blocks are now quested** - the Virtual Terrarium in `void_frogs` [gated by a KubeJS recipe override in `void_recipes.js` swapping the stock amethyst blocks for echo shards, so it can't be built until the Void frog produces them - echo shards have no other source on this skyblock; the froglight-check spirit via a void-only resource, chosen over a component-froglight ingredient because component-match recipes are unproven here] and the Slime Milk Basin in `bog_frogs` [gated by its stock packed-mud recipe]. 8 new item ids added to `item_ids.txt` [virtual_terrarium, virtual_terrarium_dome, 5 vt_upgrades, slime_milk_basin]. Dropped-by-decision [no quest]: Potion of Hopping [brews only from raw frog legs = kills a frog], Frog Legs/Soup [same], Tadpole Bucket, Sprinkler redstone switch. See [`docs/quest_coverage_audit.md`](./docs/quest_coverage_audit.md). Earlier, 2026-07-05 - **Fresh Coat** [1.24.6]: sharper Slime Bucket / Tadpole Bucket contents art plus a proper bucket base, and upgraded Slime Milk fluid textures [which tint per-variant, so every milk benefits], all backported from the 2.x line. Purely cosmetic - no recipes, data, or saves change, zero roster drift [singularities 58, froglight recipes 102, census 60/34], validator clean, dev instance re-synced to the 1.24.6 jar. Earlier, 2026-07-03 - **Deep Breath** [1.24.5]: Resource Frogs and their tadpoles no longer drown - vanilla frogs breathe underwater but PF's never could, so a frog kept in a flooded pen or a submerged farm chamber would quietly run out of air and die while the vanilla frog beside it swam on [a bug present since the mod's first release]; they now breathe underwater exactly like their vanilla cousins. Pure behavior fix, no new variants/items, zero roster drift [singularities 58, froglight recipes 102, census 60/34], validator clean, dev instance re-synced to the 1.24.5 jar. Earlier, 2026-06-30 - **Growing Pains** [1.24.4, #276/#277]: tadpoles maturing in a tight spot no longer suffocate - a frog now appears in the nearest spot it actually fits, and if there's genuinely no room the tadpole waits and grows up the moment space opens instead of dying [was: stuck-in-wall + instant suffocation; #276]; plus tadpoles can be fed **Sweetslime** to speed growth, not just slime balls [#277]. Pure behavior/QoL fix, no new variants/items, zero roster drift [singularities 58, froglight recipes 102, census 60/34], validator clean, dev instance re-synced to the 1.24.4 jar. Earlier, 2026-06-30 - **Muddy Waters** [1.24.3, #270/#271]: rewrote frog egg-laying from `blockPosition().below()` geometry to a footing-aware **contact lay** (`getBlockPosBelowThatAffectsMyMovement()`), so a bred frog lays even when standing on **mud**/slab/sub-full blocks beside the pool - fixes the mud-pen no-lay bug (Discord/Hullah + the pack's own quest copy) that made the `A Place for Frogs` / `Breed Your Frogs` mud-floor guidance wrong; pure behavior fix, zero roster drift [singularities 58, froglight recipes 102, census 60/34], validator clean. Earlier, 2026-06-29 - **Name Tags** [1.24.2]: renames the Terrarium **"Hatch" -> "Terrarium Hatch"** [display name only, same `productivefrogs:hatch` id + recipe] so it stops colliding with the two altar hatches in JEI/creative/search; pack updated the Terrarium Hatch quest title + defining refs to match. **No Crossed Wires** [1.24.1, #264]: fixed the v1.24.0 Sprinkler redstone leaking across touching Sprinklers - per-Sprinkler staging now works as the quest text says; this was the bug the pack held for before release. Re-swept at 1.24.2 with zero roster drift [singularities 58, froglight recipes 102, census 60/34]. Earlier, 2026-06-29 [1.24.0] - **Flip the Switch**: Terrarium **Sprinklers gain redstone control** [a Sprinkler pauses while powered - held milk + remaining spawns freeze and resume on signal loss - and each is controlled independently, so wire a comparator off a full chest to self-throttle, a lever to pause by hand, or stage which resources run; #263 - answers standing Discord requests (Hullah, Tavojzik) to toggle slime production vs the mob cap]. Earlier, 2026-06-29 [1.23.0] - **The Midas Touch**: an opt-in endgame **Equivalence/transmutation lane** [Alembic -> Mimic Slime -> Midas frog -> Prismatic Froglight -> Distiller; equivalent-exchange, power-hungry, Nether-Star-gated] that is **off by default** [`equivalence.enabled`] so the pack is unaffected, plus a **Wither Altar orientation fix** [it now assembles in all four orientations, was one-only; #247]. The 1.22 -> 1.24 jump adds **+1 vanilla variant `experience`** [void category]; pack sweep regenerated singularities 57 -> 58, froglight recipes 101 -> 102, vanilla census 59 -> 60 [modded census unchanged at 34], validator clean. Earlier, 2026-06-15 [1.22.0] - **Apex Predators**: two boss-tier automated farms, no new slime variants - **The End Dragon Altar** [#249; build it on the End exit portal, ring it with End Crystal Receptacles + reinforced froglights, Dragon Egg capstone, drop an end crystal into each of four receptacles and a replica dragon rises and is devoured by Dragonsbane - loot (Dragon Breath Froglight, renewable Dragon Egg Froglight, Princess's Kiss, XP) lands in the hatch; hopper-pipeable, no real boss/portal regrowth; gated `boss.dragon_altar`] and **The Wither Altar** [#247; a first Wither kill's Nether Star crafts the **Withered Star** that builds it, load four soul sand + three wither skulls into the receptacles and a replica Wither charges in a soul-forged arena until Witherbane devours it - Nether Star Froglight + XP, no boss bar or exploding blocks; gated `boss.wither_altar`]. Both harvest EXISTING boss froglight variants [nether star, dragon egg/breath, blaze rod], so zero roster change; the pack pins no boss config so both altars flow through at the boss tier. Pack sweep: all three generators reran with zero drift [singularities 57, census 34, froglight recipes 101], validator clean; 10 new content item ids [two altar hatches, three receptacles, `withered_star`, four reinforced froglights] are unquested - questing the altars is deferred and the `item_ids.txt` refresh waits for the next in-game `/sf_dump_items` dump. Earlier, 2026-06-13 - **By the Book**: PF ships its own **Patchouli field guide** [book id `productivefrogs:guide`; craft a vanilla book + slime ball; covers the whole mod incl. a Terrarium build diagram and per-altar boss diagrams], built to be **extended by modpacks** via a resource-pack merge into `assets/productivefrogs/patchouli_books/guide/` [PF #243, the pack filed it]. The pack grants it to **every player on join** via `first_join.js` [its own guard separate from the quest-book first-join guard, so pre-existing saves receive it once too] and it stays craftable. Also adds the **Sweetslimed Lily Pad perch** [right-click a placed lily pad with a Sweetslime; a Resource Frog claims it and holds position - pin one over a hopper by a milk source for hands-off collection; config `lily_pad_perch.enabled` default on, `lily_pad_perch.range`; #214] - no pack action, questing deferred. **Breeding reworked**: an offspring stat is now the **average of its parents** then a chance to climb one above; **regression removed** [`breeding.regressionChance` gone] and **every breed improves at least one stat** [`breeding.guaranteedImprovement`, default on]. The pack pins no breeding config so the new model flows through; the **Raising the Line** quest text was rewritten to match [the v1.20-era 2-in-5/3-in-10 odds the editorial pass set are now stale]. Boss-altar self-spawn fix [#242], no pack impact. Zero roster drift [singularities 57, census 34, froglight recipes 101]. Earlier, 2026-06-10 [1.20.0] - **Full Bloom**: PF's stable 1.0 milestone, out of beta - no gameplay change in 1.20.0 itself, but the 1.19.1 -> 1.20.0 jump folds in **1.19.2**: the Jade tadpole "Growing time" readout fix [PF #238, the pack filed it], the Slime Bucket water-on-release leak fix, fluids no longer washing away milk pools, and a standalone Advancements tab [complementary to the quest book]. No new variants/items, zero roster drift [singularities 57, census 34, froglight recipes 101]. Earlier, 2026-06-10 [1.19.1] - **Survival of the Fittest**: tuning only, no new variants/items - the default frog-stat improvement chance rises 0.20 -> 0.40 per stat (a breed improves at least one of the three stats ~78% of the time, up from ~49%); the pack doesn't pin `breeding.improvementChance`, so the new default flows through from the jar, zero roster drift [singularities 57, census 34, froglight recipes 101]. Earlier, 2026-06-10 [1.19.0] - **Stone Soup**: content additions, no new variants/items - Lava Froglights now burn as furnace fuel [20,000 ticks = one lava bucket; per-variant, others stay inert], and the Froglight Crucible now melts cobblestone/stone/gravel [250 mB] + netherrack [500 mB] straight to lava [Ex Deorum parity, heat required]. Automatic/data-side, no pack action; sweep confirmed zero drift [singularities 57, census 33, froglight recipes 101]. Earlier, 2026-06-10 [1.18.0] - **Made to Measure**: a config-only release [no new variants/items] - master switches for frog stat-breeding [`frog_stats.enabled`] and the boss tier [`boss.enabled`], plus a `[variants]` section to disable individual variants, whole species, or a host mod's variants [`disabledVariants` / `disabledCategories` / `disabledIntegrations`]. All default-on, so the pack is unaffected; the sweep confirmed zero drift [singularities 57, census 33, froglight recipes 101]. Earlier, 2026-06-09 [1.17.0] - **Frog Legs and Fairy Tales**: a content release with no new slime variants - Frog Legs + Soup [renewable meat from killed frogs], the Frog Net [catch/relocate a Resource Frog stats-intact], the Froglight Cleaver [endgame sword from boss Froglights], Princess's Kiss [dragon drop -> right-click a frog to make it a villager], Potion of Hopping, plus config toggles for brewed froglights / milker / churn / crucible / mold / per-catalyst. Terrarium: Incubators now optional [forms with zero], Hatch collects raw frog legs, Controller GUI shows live count not `count/cap`. Crucible plastic + pink_slime melts now yield exactly 1000 mB [pack #223, closed upstream]. Pack sweep: roster unchanged so singularities/census untouched [still **57**], 6 new item ids added to `item_ids.txt`, the Terrarium chapter text swept; **questing the new content + config-toggle decisions are deferred**. Earlier, 2026-06-08 [1.16.0] - **The Terrarium**: a sealed 5x4x5 multiblock that automates the whole frog loop [Controller funnels Slime Milk to ceiling Sprinklers that rain slimes, Incubators raise stat-intact frogs, a Hatch collects Froglights]. Four blocks [`terrarium_controller`, `sprinkler`, `incubator`, `hatch`], all Infernal-gated by recipe; build aid is the Controller's right-click validation [no GuideME guide yet]. The pack adds **The Terrarium chapter** [`terrarium.snbt`, Infernal group order 3]. 1.15.0 brought the Slime Churn + Just Dire Things support [7th Sister Ponds column, 33 modded census quests]; vanilla roster has held at 59 since 1.14. 1.14.0 brought Brewed Froglights [potion auras + Curios slot], four BOSS resources [wither skull/nether star Infernal, dragon egg/breath Void - altar-gated upstream, self-keyed chamber rows pack-side so the real drop is always spent], five mob-drop vanilla stragglers, Mekanism refined_glowstone. The fluid pair water/lava stays excluded - #131, kelp/pointed_dripstone have no item source, exclusion lives in `gen_singularities.py`. The full bump-by-bump history (every version since 1.0.1, what each brought, the 1.8.0 breaking milk split) lives in [`docs/pf_pin_history.md`](./docs/pf_pin_history.md). PF ships no slime-milk-bucket tag, so the pack's `slime_milk_tags.js` stays load-bearing. Pull newer files with `packwiz update productive-frogs`.

## Project Conventions

- **Docs filenames:** snake_case (e.g. `design_overview.md`), per the global rule.
- **Line endings:** `.gitattributes` forces LF for source/config/docs and CRLF for `.bat`/`.cmd`/`.ps1`. Don't fight it.
- **Conventional Commits:** `feat:`, `fix:`, `refactor:`, `docs:`, `chore:`, `ci:`. Body explains the **why**. `main` is protected; changes via PR.
- **License:** MIT for pack-authored content. Bundled mods retain their own licenses.

## Scope discipline

This repo is a **content pack**. Java code does **not** belong here - if a feature would require a Java mod, it goes in [productive-frogs](../productive-frogs) instead.

Acceptable surfaces inside this repo:
- KubeJS scripts (`kubejs/{server,startup,client}_scripts/`)
- Datapack overrides (`kubejs/data/<ns>/`)
- Resourcepack overrides (`kubejs/assets/<ns>/`)
- Mod configs (`config/`, `defaultconfigs/`)
- FTB Quests data (`config/ftbquests/quests/`)
- Pack metadata (`pack.toml`, `index.toml`, `manifest.json`)
- Pack-level docs (`docs/`, `README.md`)

If something needs new Java behavior, file an issue against Productive Frogs and reference the pack-side need.

## Design decisions (settled)

These were once open "working assumptions"; they are now **settled and built**. Kept here as the load-bearing decisions a contributor must respect, with the original framing where the history matters:

1. **Frogs as the spine of resource generation.** Whether this means "frogs replace mining entirely" or "frogs are the headline path among several" is unresolved. See [`docs/design_overview.md`](./docs/design_overview.md).
2. **Six categories follow Productive Frogs.** This one is the closest to canonical because it inherits from the mod's enum - but the *progression order* and tier-mapping are open. Settled 2026-05-25: PF v1.0.0's species-as-category redesign means the enum is now BOG / CAVE / GEODE / TIDE / INFERNAL / VOID (biome-species names, player-facing). Sky Frogs adopts these names; the tier mapping is now **settled and built** as Tier 1 = Cave, Tier 2 = Geode, Tier 3 = Bog, with Tier 4 = Tide **fully built** (`road_to_tide` bridge + `drowned_riches` species chapter + `take_flight` verb chapter; the Tide verb is **jetpacks/mobility** via Iron Jetpacks, the pack's first mobility verb), and **Tier 5 = Infernal fully built** (2026-05-31: `road_to_infernal` gateway + `infernal_frogs` species chapter [quartz->glowstone->soul_sand->soul_soil->blaze->netherite_scrap capstone] + `the_network` verb chapter; the Infernal verb is **Refined Storage** [RS 2.0, the digital-storage + autocrafting network], gated on nether quartz which only the Infernal frog produces - the nether-on-skyblock tier. **Tier 6 = Void fully built** (2026-06-01: `road_to_void` gateway + `void_frogs` species chapter [ender_pearl in the gateway, then end_stone->chorus_fruit->echo_shard->sculk->shulker_shell capstone] + `the_ultimate_table` verb chapter + `master_pond` endgame; the Void verb is **Extended Crafting** [singularity engine, pinned 7.0.8], the gate is a one-time **dragon expedition** [craft a 12-frame End portal from frog-farmed glowstone+soul_sand since no stronghold generates, enter the End, slay the dragon], and the endgame compresses one **Singularity per vanilla froglight resource** (one per vanilla variant - 57 as of PF 1.14, growing with PF's roster; the FLUID PAIR water/lava is excluded by maintainer ruling #131 [their primer items kelp/pointed_dripstone have no source here] - the exclusion lives in the generator; `config/extendedcrafting/singularities/*.json`, generated by `tools/gen_singularities.py` from PF's `slime_variant` data, frog-matched colors) in the Quantum Compressor - 1000 of each resource, a proof you automated every farm - then folds the full set into the **Ultimate Singularity** (EC ships zero default singularities, so the auto-generated Ultimate needs exactly this set) -> the KubeJS-defined **Master Frog** trophy [placeholder texture; final art is a backlog item]. The compressor input is the smelted resource, not the Froglight directly: EC's singularity `ingredient` and the compressor's Cucumber `IngredientWithCount` input are both item/tag-only and reject `neoforge:components`, and a tag can't help since all variants share one item id - so per-variant froglight singularities are impossible without an upstream PF item-split; on this skyblock each resource's only source is its frog, so the singularities stay froglight-gated. The content campaign is now complete; the vanilla Void variants are quested (the six chain variants, plus the off-chain `experience`/XP variant added in v1.3.0); modded Void variants deferred. **Merged to `main` 2026-06-02 (PR #47).** Two pack-wide design laws settled earlier: per-tier resource quests check the variant Froglight rather than the smelted resource (so the frog loop can't be bypassed; iron-ingot capstone and the main Mekanism steel quest are the exceptions), and each tier's slime-chain recipe uses a tier-themed filler block (Cave = stone, Geode = gravel, Bog = mossy cobblestone, Tide = mycelium, Infernal = prismarine, Void = soul_soil - Tide/Infernal/Void deliberately break the "filler is a plain non-resource block" precedent by reusing a *prior* tier's mass-renewable chain resource [netherrack would be circular for Infernal, end_stone for Void]).
3. **Cross-mod compat via datapack JSONs.** Inherited from how Productive Frogs already works; the pack-side workflow for generating those JSONs is unresolved.
4. **~~Hand-operated appliances only in v0.x.~~ SUPERSEDED 2026-06-08 (PF 1.16).** PF shipped automation in the 1.x line ("V2 is just a name, not a rule") with **The Terrarium** - a sealed multiblock that runs the frog loop hands-off. The pack embraces it: the Terrarium is the first *automated* frog appliance, Infernal-gated (recipe needs quartz/glowstone/blaze/magma/nether brick) and quested in its own Infernal chapter (`terrarium.snbt`). Hand-operated appliances remain the early-tier norm; automation is the Infernal-tier payoff, not a v1.0 gate.

If one of these is ever revisited and changed, update it here and reflect the change in the affected docs (and the CHANGELOG if it alters gameplay).

## Common commands

`pack/` is initialized (packwiz, MC 1.21.1 / NeoForge 21.1.248). All packwiz commands run from inside `pack/`:

```sh
packwiz refresh                          # regenerate index.toml after editing any .pw.toml
packwiz cf add <curseforge-slug>         # add a mod (CurseForge - sole distribution channel)
packwiz remove <mod-slug>                # remove a mod
packwiz update --all                     # pull latest versions of all pinned mods
packwiz curseforge export                # build the .zip for CurseForge
```

`packwiz mr add` and `packwiz modrinth export` are intentionally unused - see "Distribution" in Versioning targets above.

**After the game has loaded a world** (FTB Quests and some mods rewrite their data files to CRLF on Windows), run `python tools/pack_refresh.py` from the repo root **instead of** bare `packwiz refresh`. It normalizes the LF-governed pack files (`*.snbt`, configs, etc.) back to LF on disk, then refreshes, so `index.toml` records hashes that match the committed LF blobs. Running bare `packwiz refresh` against CRLF disk state writes hashes that no committed file has (the pack's own integrity index goes silently wrong).

**After a mod pin changes** (`packwiz cf add` / `update` / `remove`), the junction-linked dev instance keeps running the old jars - its `mods/` folder is *not* junctioned (packwiz tracks metadata, not jars). With Minecraft closed, run `python tools/sync_instance.py` from the repo root to mirror the pinned mods into the instance. It drives the canonical `packwiz-installer` (auto-picks a free port, `packwiz serve`, then the official installer jar), which downloads each pin from source, hash-verifies it, and does clean add/update/remove - and refuses to run while Minecraft holds the jar locks. `--side both` to include server-only mods; `--instance <path>` to target a different instance.

**After the NeoForge pin changes** (`[versions] neoforge` in `pack.toml`), run `python tools/sync_instance_loader.py` (add `--apply` to write it). `sync_instance.py` mirrors **jars only**; nothing syncs the loader, so a bump otherwise leaves the instance behind and every mod requiring the newer build dies at load with "Mod X requires neoforge A or above / Currently, neoforge is B" - not a pack bug, and not debuggable as one.

Three things each silently defeat the change on their own, and all three were hit during the v1.5.3 bump:

1. **The loader is stored twice, and the app-level store wins.** `baseModLoader` lives in the instance's `minecraftinstance.json` *and* in `%LOCALAPPDATA%\Overwolf\Curse\GameInstances\MinecraftGameInstance.json`. CurseForge rewrites the per-instance file from the app-level one on launch, so patching only the instance file reads back correctly and is then silently reverted - a convincing false success. The helper writes both.
2. **CurseForge must be fully closed**, including the Overwolf tray icon. While running it holds every instance in memory and writes them out on launch. The helper refuses to `--apply` if it sees the processes.
3. **Never hand-install the loader.** CurseForge installs it from its own catalog on the next launch. Pre-placing files under `Install/versions/` and `Install/libraries/` (e.g. by running the NeoForge installer) makes CurseForge's install task fail outright - `InstallTaskFailedException`, surfacing as "Failed to launch modpack. An unexpected error occurred." It finds a partial install it did not create and gives up. Deleting those files fixes it.

The helper takes the `baseModLoader` block from CurseForge's own API (`/v1/minecraft/modloader/neoforge-<version>`) rather than reconstructing it, so it is byte-for-byte what CurseForge would write.

**Watch which instance.** The CurseForge display name is not the folder name: the junction-linked dev instance lives in `Instances\Sky Frogs` but shows as **"Sky Frogs (DEV)"**, while `Instances\Sky Frogs (1)` shows as plain **"Sky Frogs"** and is *not* junctioned. The helper prints both.

This bit on the 21.1.233 -> 21.1.244 bump in v1.5.3: the instance was found on **21.1.230**, two bumps behind, so local playtests had never once run the loader the pack shipped.

`tools/` helper scripts that exist: `fix_quest_ids.py` (remap negative-leading quest IDs after editing a chapter), `validate_quests.py` (static FTB Quests validator - run after any chapter/lang edit; catches the tier-skip / dangling-dep / bad-id / em-dash / quest-recipe-drift classes, the PF-jar drift classes [singularity-ingredient + dissolution-threading vs the pinned jar, #101], the chain-mirror classes [table chains vs the chamber, #125; the ATO chain vs the modded rows, #126 review], and the jetpack-coil drift class [Take Flight's copy vs the live Iron Jetpacks tier ladder, #233]; see [`docs/quest_testing.md`](./docs/quest_testing.md)), `gen_singularities.py` (regenerates the EC singularity JSONs from PF variant data - rerun on PF variant/resource changes), `gen_completionist_chapters.py` (regenerates the two census chapters from PF variant data + loaded mods - rerun alongside it; ids are deterministic so completion survives), `gen_froglight_slime_recipes.py` (regenerates the Froglight -> Slime-in-a-Bucket crafting recipes for every variant from PF variant data - rerun on PF variant changes; emits `kubejs/server_scripts/froglight_slime_recipes.js`), `pf_jar.py` (shared pinned-jar access for the three above), `ironjetpacks_tiers.py` (computes which coil each jetpack takes from the Iron Jetpacks configs, since the mod assigns them by tier POSITION not by name - backs Q-JETPACK-COIL; rerun the validator after any mod-list or `config/ironjetpacks/` change), `score_quest_voice.py` (scores every quest description for mechanical AI-tells and regenerates `docs/quest_rewrite_candidates.md`; rerun after any quest-text edit - the formula lives in the script header), `gen_starter_island.py`, `pack_refresh.py` (above), `sync_instance.py` (above), `sync_instance_loader.py` (above - checks, and with `--apply` sets, the dev instance's NeoForge loader against the pack pin; writes both CurseForge stores, refuses while CurseForge is running, and never hand-installs the loader), `check_pack_configs.py` (verifies every FML-managed mod-config override exists byte-identically in both `pack/config/` and `pack/defaultconfigs/`, so it survives NeoForge recreating the file; `--fix` syncs), `check_server_pack_flag.py` (audits whether each CurseForge release's server pack is *typed* as a Server Pack, not just attached - the flag can only be set by hand in the Authors Console, so this is the post-release check; see [`docs/distribution.md`](./docs/distribution.md)). `build_cf_zip.sh` and `slime_variant_codegen.py` are still planned per [`docs/repo_layout.md`](./docs/repo_layout.md) but not yet created.

**Mod-config overrides go in BOTH `pack/config/` and `pack/defaultconfigs/`.** `config/` is live per-instance state, written once when the pack is installed; if NeoForge ever has to recreate the file (it backs the old one up to `<name>-N.toml.bak` when it cannot be parsed or restored) the pack's values are gone and the mod's own defaults take over, silently. `defaultconfigs/` is what NeoForge seeds a missing config from - verified in the pinned loader (fancymodloader 4.0.43): `ConfigTracker` copies `defaultconfigs/<name>.toml` when the config file does not exist, and only falls back to generating from the mod's spec if no default is supplied. So a value that lives only in `config/` is not durable. Run `python tools/check_pack_configs.py` (`--fix` syncs) after touching any override; the `validate-pack` workflow enforces byte-identity. Applies to FML-managed TOMLs only - `botanypots.json` and other mod-owned formats never consult `defaultconfigs/` and are excluded by name in the script.

Quest-authoring guardrail: after editing any `config/ftbquests/quests/` file, run `python tools/validate_quests.py` (a clean exit is required; the `.githooks/pre-commit` hook and the `validate-quests` CI workflow enforce it). The in-game runtime smoke test is `/sf_selftest` (op level 2) after a `/reload`.

Releases are tag-driven: bump `pack/pack.toml` + update `CHANGELOG.md`, then `git tag v0.x.y && git push origin v0.x.y` fires `.github/workflows/release.yml` (builds the CF client + server packs, GitHub release, CurseForge upload, Discord post). Follow [`docs/release_checklist.md`](./docs/release_checklist.md) step by step; [`docs/distribution.md`](./docs/distribution.md) has the why.
